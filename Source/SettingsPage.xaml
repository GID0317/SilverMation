<UserControl x:Class="SilverMation.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SilverMation"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextColor}"/>

        <Border Padding="15,12,12,12" Background="{DynamicResource ExpanderHeaderBackground}" CornerRadius="{DynamicResource ControlCornerRadius}" Margin="0,10,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" MinWidth="200" />
                    <ColumnDefinition Width="5*" MinWidth="200" />
                </Grid.ColumnDefinitions>

                <DockPanel>
                    <StackPanel Margin="1,2,0,3" VerticalAlignment="Center">
                        <TextBlock Text="Choose app theme" FontSize="14" FontWeight="Normal" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <TextBlock Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" TextTrimming="CharacterEllipsis" TextWrapping="WrapWithOverflow" Text="Select which app theme to display"/>
                    </StackPanel>
                </DockPanel>

                <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged" FontWeight="Regular" Grid.Column="1" HorizontalAlignment="Right" Margin="0, 3, 0, 0">
                    <ComboBoxItem Content="Light" FontWeight="Regular"/>
                    <ComboBoxItem Content="Dark" FontWeight="Regular"/>
                    <ComboBoxItem Content="Use system setting" FontWeight="Regular"/>
                </ComboBox>
            </Grid>
        </Border>

        <TextBlock Text="About" Margin="10" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextColor}"/>
        <Expander x:Name="Expander_Language" IsExpanded="True" Style="{StaticResource {x:Static ui:ThemeKeys.ExpanderCardStyleKey}}" HorizontalAlignment="Stretch">
            <Expander.Header>
                <Grid VerticalAlignment="Center" Margin="0,10,-10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="AppNameTxt" Text="SilverMation" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2" FontSize="14" FontWeight="Normal" Foreground="{DynamicResource PrimaryTextColor}"/>
                    <TextBlock x:Name="AppCopyrightTxt" Text="Copyright © GID Under MIT License. All rights reserved." Grid.Column="1" VerticalAlignment="Top" Grid.Row="1" Margin="0,2,20,5" Opacity="0.615" FontSize="12" TextTrimming="CharacterEllipsis" TextWrapping="WrapWithOverflow" Foreground="{DynamicResource PrimaryTextColor}"/>

                    <TextBox Text="Version 1.1.0" FontWeight="Regular" FontSize="14" BorderThickness="0" Background="Transparent" IsReadOnly="True" IsReadOnlyCaretVisible="False" Foreground="{DynamicResource PrimaryTextColor}" Grid.Column="2" HorizontalAlignment="Right" Grid.RowSpan="2" VerticalAlignment="Center"/>
                </Grid>
            </Expander.Header>

            <Grid Margin="5,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="FeedBackTxt" Text="Send a Feedback File a bug or contribute to the project" Margin="0,10,0,5" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>

                <ui:HyperlinkButton Content="Go to the page" FontWeight="Regular" NavigateUri="https://github.com/GID0317/SilverMation" Margin="0,5,0,5" Grid.Column="1"/>
            </Grid>
        </Expander>
    </StackPanel>
</UserControl>
